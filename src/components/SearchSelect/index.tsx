import React, { FC, useEffect } from 'react';
import { Select } from 'antd';
import { InternalSelectProps } from 'antd/lib/select';

const SearchSelect: FC<SearchSelectProp> = (props: SearchSelectProp) => {

    return (
        <Select>
        </Select>
    );
};

export default SearchSelect;